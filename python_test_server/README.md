## Python test server

This creates a simple HTTP server on localhost:8081 which will acceot any input and return 200.

It will log the request to http://localhost:8081:monitor so that you can test the main router application.

